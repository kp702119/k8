# Product Service - Secrets Flow Diagram

## 🔄 Architecture Overview

### Before: Hardcoded Secrets
```
┌─────────────────────────────────────────────────────────┐
│           Developer's Machine                            │
│  ┌──────────────────────────────────────────────────┐   │
│  │  appsettings.Development.json                    │   │
│  │  {                                               │   │
│  │    "DefaultConnection": "Password=1n9pp2.0@..." │   │ ❌ EXPOSED
│  │    "RabbitMq": { "Password": "RabbitMQ@..." }   │   │
│  │  }                                               │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                        ↓
                    Git Push
                        ↓
        ❌ Secrets in GitHub Repository

┌─────────────────────────────────────────────────────────┐
│           Kubernetes Cluster                             │
│  ┌──────────────────────────────────────────────────┐   │
│  │  secret.yaml (hardcoded in YAML)                │   │
│  │  stringData:                                    │   │
│  │    DefaultConnection: "...Password=..."        │   │ ❌ EXPOSED
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

---

### After: Vault Integration (Secure)
```
┌──────────────────────────────────────────────────────────────────┐
│                    LOCAL DEVELOPMENT                              │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  appsettings.Development.json                             │  │
│  │  {                                                        │  │
│  │    "DefaultConnection": "${ConnectionStrings__Default}"  │  │ ✅ SAFE
│  │    "RabbitMq": { "Password": "${RabbitMq__Password}" }   │  │
│  │  }                                                        │  │
│  └────────────────────────────────────────────────────────────┘  │
│                            ↓                                      │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  .env.local (NOT IN GIT - .gitignore)                     │  │
│  │  ConnectionStrings__DefaultConnection=...                │  │
│  │  RabbitMq__Password=...                                  │  │
│  └────────────────────────────────────────────────────────────┘  │
│                            ↓                                      │
│              dotnet run (with env vars)                          │
└──────────────────────────────────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────┐
│                 VAULT (Central Secret Store)                      │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  secret/wingyip-srs/staging/product/                       │  │
│  │  ├── database/connectionstring                            │  │
│  │  ├── services/administrationservice                       │  │
│  │  ├── services/spacemanservice                             │  │
│  │  ├── elasticsearch/url                                    │  │
│  │  └── ...                                                  │  │
│  ├────────────────────────────────────────────────────────────┤  │
│  │  secret/wingyip-srs/staging/shared/                        │  │
│  │  └── rabbitmq/(hostname, port, user, pass)                │  │
│  └────────────────────────────────────────────────────────────┘  │
│     ↑                                                ↑             │
│     └──────── Only DevOps can write ────────────────┘             │
│  Vault Address: http://10.10.80.77:30820                         │
└──────────────────────────────────────────────────────────────────┘
         ↑                          ↑
         │                          │
    (Setup Once)            (K8s reads)
         │                          │
         │            ┌─────────────┘
         │            │
         ▼            ▼
    ┌─────────────────────────────────────────────────────────────┐
    │  store-staging-secrets-in-vault.sh                          │
    │  (Run: bash store-staging-secrets-in-vault.sh)              │
    └─────────────────────────────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────┐
│            KUBERNETES CLUSTER (Staging/Prod)                      │
│                                                                    │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  ExternalSecret (external-secrets.yaml)                   │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │ Watches Vault every 1 hour                           │ │  │
│  │  │ Pulls: ConnectionStrings__DefaultConnection          │ │  │
│  │  │        RabbitMq__HostName, Port, User, Pass          │ │  │
│  │  │        Services__* (7 URLs)                          │ │  │
│  │  │        ElasticSearch__* (3 configs)                  │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  └────────────────────────────────────────────────────────────┘  │
│                            ↓                                      │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  K8s Secret: productservice-secret                        │  │
│  │  (Auto-populated by ExternalSecret)                       │  │
│  │  ✅ NO HARDCODED VALUES                                   │  │
│  └────────────────────────────────────────────────────────────┘  │
│                            ↓                                      │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  ConfigMap: productservice-config                         │  │
│  │  (Non-sensitive configuration only)                       │  │
│  │  - ASPNETCORE_ENVIRONMENT: Production                     │  │
│  │  - TransportType: Http                                   │  │
│  │  - Logging levels                                        │  │
│  └────────────────────────────────────────────────────────────┘  │
│                            ↓                                      │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  Pod Environment Variables (Injected)                     │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │ FROM ConfigMap:                                      │ │  │
│  │  │  - ASPNETCORE_ENVIRONMENT = Production              │ │  │
│  │  │  - Logging__LogLevel__Default = Information         │ │  │
│  │  │                                                      │ │  │
│  │  │ FROM Secret (via ExternalSecret from Vault):         │ │  │
│  │  │  - ConnectionStrings__DefaultConnection = <vault>   │ │  │
│  │  │  - RabbitMq__HostName = <vault>                     │ │  │
│  │  │  - RabbitMq__Password = <vault>                     │ │  │
│  │  │  - Services__AdministrationService = <vault>        │ │  │
│  │  │  - ... (7 more service URLs)                        │ │  │
│  │  │  - ElasticSearch__Url = <vault>                     │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  └────────────────────────────────────────────────────────────┘  │
│                            ↓                                      │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  ASP.NET Core Application                                │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │ Configuration System:                                │ │  │
│  │  │  1. Read appsettings.json (placeholders)             │ │  │
│  │  │  2. Override with environment variables              │ │  │
│  │  │  3. Connect to database using actual connection str  │ │  │
│  │  │  4. Connect to RabbitMQ using actual credentials     │ │  │
│  │  │  5. Query ElasticSearch using actual URL             │ │  │
│  │  │                                                       │ │  │
│  │  │  Result: ✅ Fully configured with secrets from Vault │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Sequence

```
┌─────────────────────────────────────────────────────────────────┐
│                        SETUP PHASE (One-time)                    │
└─────────────────────────────────────────────────────────────────┘

  DevOps Team
       │
       ├─► 1. bash store-staging-secrets-in-vault.sh
       │
       ▼
  HashiCorp Vault
       │
       ├─ secret/wingyip-srs/staging/product/database
       ├─ secret/wingyip-srs/staging/product/services
       ├─ secret/wingyip-srs/staging/product/elasticsearch
       └─ secret/wingyip-srs/staging/shared/rabbitmq
       │
       └─► Secrets stored (encrypted)


┌─────────────────────────────────────────────────────────────────┐
│                   DEPLOYMENT PHASE (Per deployment)              │
└─────────────────────────────────────────────────────────────────┘

  DevOps Team
       │
       ├─► kubectl apply -f external-secrets.yaml
       ├─► kubectl apply -f configmap.yaml
       ├─► kubectl apply -f secret.yaml
       └─► kubectl apply -f deployment.yaml
       │
       ▼
  Kubernetes Cluster
       │
       ├─ ExternalSecret created
       │  (watches Vault every 1 hour)
       │
       └─ Deployment started
          ├─ Waits for Secret to be ready
          │
          ▼
       ExternalSecret Operator
          │
          ├─► Polls Vault
          ├─► Retrieves all secrets
          └─► Creates/Updates K8s Secret
          │
          ▼
       K8s Secret: productservice-secret
          │
          ├─ ConnectionStrings__DefaultConnection: <value>
          ├─ RabbitMq__HostName: 10.10.80.77
          ├─ RabbitMq__Password: <value>
          ├─ Services__AdministrationService: <value>
          └─ ... (13 total keys)
          │
          ▼
       Pod Starts
          │
          ├─ Reads ConfigMap → Env Vars
          ├─ Reads Secret → Env Vars
          │
          ▼
       ASP.NET Core App
          │
          ├─ Loads appsettings.json (with placeholders)
          ├─ Overrides with Environment Variables
          │
          ▼
       ✅ Fully configured!
          ├─ Database: Connected ✓
          ├─ RabbitMQ: Connected ✓
          ├─ Services: URLs resolved ✓
          └─ ElasticSearch: Connected ✓


┌─────────────────────────────────────────────────────────────────┐
│              RUNTIME PHASE (Continuous operation)                │
└─────────────────────────────────────────────────────────────────┘

  ExternalSecret Operator
       │
       └─► Every 1 hour:
           ├─ Poll Vault
           ├─ Check for changes
           │
           └─► If secrets changed:
               ├─ Update K8s Secret
               │
               └─► Pod gets signal
                   ├─ Env vars updated
                   └─ App can restart/reload config
                      (if configured)

  Manual Secret Update
       │
       ├─► DevOps: vault kv put secret/.../database
       │   (password expires or needs rotation)
       │
       └─► Within 1 hour:
           ├─ ESO pulls new value
           ├─ K8s Secret updated
           ├─ Pod env var refreshed
           └─ ✅ No downtime!
```

---

## 🔐 Security Improvements

```
BEFORE                              AFTER
──────────────────────              ──────────────────────

❌ Secrets in Git                   ✅ Secrets NOT in Git
   ├─ Anyone with repo access       ├─ Only Vault stores secrets
   └─ Vulnerable to disclosure      └─ Encrypted at rest

❌ Hardcoded in YAML                ✅ Secrets in Vault only
   ├─ K8s Secret readable           ├─ ESO manages SecureString
   └─ etcd readable                 └─ Audit trail available

❌ Manual secret updates             ✅ Automated secret sync
   ├─ Requires rebuilding image     ├─ No image rebuild needed
   └─ Risk of forgotten updates     └─ Within 1 hour always synced

❌ No audit trail                   ✅ Full Vault audit logs
   ├─ Who accessed secrets?         ├─ Every Vault access logged
   └─ What changed when?            └─ When and who

❌ No access control                ✅ Vault policies enforce
   ├─ All pods see all secrets      ├─ Only authorized pods
   └─ Horizontal privilege climb    └─ Least privilege principle

❌ Local dev insecure               ✅ Local dev flexible
   ├─ Store in .env (risky)         ├─ .env.local in .gitignore
   └─ Share via chat/email          └─ Can use Vault too
```

---

## 📈 Timeline

```
Time    Action                      Impact
────────────────────────────────────────────────────────────

Day 0   • Store secrets in Vault
        • Update files
        • Push to Git
        ✅ No secrets in Git

        • Deploy ExternalSecret
        • Deploy ConfigMap/Secret
        • Deploy new Deployment
        ✅ K8s reading from Vault

Day +1  • Developers pull changes
        • Run with environment variables
        ✅ Local development secure

Day +7  • Review logs, confirm working
        ✅ No incidents

Day +30 • Rotate database password
        • Update in Vault
        • Within 1 hour, pods refresh
        ✅ Zero downtime rotation

Ongoing • Monitor Vault access logs
        • Ensure ESO is syncing
        • Track secret access
        ✅ Secure and auditable
```

---

## 🎯 Key Takeaways

1. **No More Secrets in Git** ✅
   - appsettings files are now safe to commit
   - Placeholders only, actual values from environment

2. **Centralized Secret Management** ✅
   - All secrets in Vault
   - Single source of truth
   - Easy to rotate and audit

3. **Zero Downtime Updates** ✅
   - Change Vault → ESO syncs → Pods refresh
   - No need to rebuild images
   - No need to redeploy pods (just pod restart if app supports it)

4. **Developer Friendly** ✅
   - Simple environment variable setup for local dev
   - Same configuration approach as production
   - No special tools needed

5. **Audit & Compliance** ✅
   - Full audit trail in Vault
   - Know who accessed what and when
   - Meet compliance requirements

---

**Created:** January 8, 2026
**Status:** ✅ Ready for use
